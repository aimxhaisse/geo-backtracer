---

### Common packages to install.

- name: set up classic packages
  apt:
    name:
      - htop
      - git
      - ufw
      - tree
      - emacs-nox
      - tmux
      - apt-transport-https
      - python3-jinja2
    state: present
