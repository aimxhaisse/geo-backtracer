---

### Common packages to install.

- name: set up classic packages
  apt:
    name:
      - htop
      - nmap
      - git
      - ufw
      - tree
      - emacs-nox
      - tmux
      - apt-transport-https
      - python3-jinja2
    state: present
    update_cache: yes
    cache_valid_time: 86400


